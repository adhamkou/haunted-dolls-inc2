<div class="container">
<h1>Make a booking </h1>
<%= simple_form_for([@doll,@booking]) do |f| %>
  <%= f.input :booking_start_date %>
  <%= f.input :booking_end_date %>
  <%= f.submit 'Submit', class: 'btn btn-primary' %>
<% end %>
<%# <%= link_to "Back to booking", list_path(@list) %>
</div>
